# 社内承認回覧Webアプリ 引継ぎ文書

最終更新：2026-08-30
対象プロジェクト：`approval-circulation-app`
旧PC上のパス：`C:\Users\win1\Documents\app\approval-circulation-app`

## 1. この文書の役割

この文書は、会話履歴が存在しない新しいPC・新しいCodexスレッドでも、ユーザーの指示、設計判断、UI調整の意図、実装状況、未完了作業を失わずに再開するための正本です。

仕様が衝突した場合の優先順位：

1. この文書の「最新確定仕様」
2. ユーザーが新しいスレッドで追加した最新指示
3. 現在のコードで確認できる動作
4. 最初に提示された全体要件

最初の要件から後で明示的に変更された事項は、後の判断を優先します。特に「Microsoft Entra IDで利用者ログイン」は取りやめ、「本人確認なし・案件専用URL方式」が最新仕様です。

## 2. ユーザーが目指している最終像

- 社内のPDF、Excel、Word、PowerPoint、その他業務ファイルを順番に承認回覧するWebアプリ。
- PDFとExcelを最優先する。
- SharePointとOneDriveを正式対応する。
- 将来はWindows/SMB共有フォルダも、バックエンドや社内エージェントを介して対応する。
- 最終的には会社PCを社内Webサーバーとして運用する。
- 各案件に専用URLを1つ発行し、そのURLをメールで次の確認者へ送る。
- 案件URLの先頭画面で、現在誰が承認待ちなのかが一目で分かる。
- 本人確認や利用者ログインは行わない。URLを受け取った人が処理できる、シンプルな方式を優先する。
- 承認・差し戻し後はWebアプリ内でメール本文を確認・編集し、送信ボタンで送信する。`mailto:`によるメーラー起動は採用しない。
- 初期段階は操作感を確認できるローカルUIを最優先し、Microsoft 365接続は後から段階的に追加する。

## 3. 最新確定仕様

### 3.1 名称と用途

- 表示名は「社内承認回覧」。
- 「Circlia」を広い製品名として見せず、社内承認回覧用途だけに絞る。
- 中途半端な英語表記は避け、日本語で統一する。

### 3.2 認証と案件URL

- 利用者の本人確認は不要。
- Microsoft 365ログイン、MSALによる利用者ログイン、個人判定は行わない。
- 1案件ごとにランダムな`accessKey`を発行し、URLを割り当てる。
- URL例：`https://<社内ホスト>/c/<ランダムアクセスキー>`
- 連番案件IDはURL認可情報として使用しない。
- 案件URLはメールで共有する。
- URLを知る人は転送・処理できる。これは現仕様の意図的な簡素化であり、将来必要なら有効期限、再発行、アクセスログ、PIN等を検討する。

### 3.3 メール

- 「回覧を開始」「確認完了して回付」「捺印・確認完了して回付」「差し戻し」「再回覧」では、案件や回付ルートを変更する前にWebアプリ内メール作成画面を開く。
- 宛先、件名、本文、案件URL、現在の確認者等を自動作成する。
- その時点までに回付へ関わった承認者をCCへ自動入力する。同じ人が複数回登場してもメールアドレス単位で1件にまとめ、宛先と同じアドレスはCCから除外する。
- ブラウザ上で編集できる。
- 送信ボタンで送るUIにする。
- 「送信を確認」を押したときだけ案件、処理履歴、現在の承認待ちを更新する。キャンセルした場合はメールを送らず、いずれも変更しない。
- STEP 1の公開デモでは送信確認のみで、実メールは送らない。
- 実メール送信方式はSTEP 13で、管理者設定を必要としない方法を優先して再検討する。Microsoft Graph `sendMail`を前提に固定しない。
- 差し戻しコメントが空欄なら、メールには「コメント：なし」と表示する。

### 3.4 UIデザイン

- 全体は明るい水色基調。
- PCブラウザ優先。スマートフォン最適化は補助。サイドバーや開閉メニューは使わない。
- ホーム画面に案件一覧をそのまま表示し、上部に「新しい回覧」「承認者マスタ」の2ボタンを置く。
- 「新しい回覧」と「承認者マスタ」は別ウィンドウで開く。別ウィンドウで変更したLocalStorageの内容は、開いているホームへ自動反映する。
- 業務用途として簡潔で見やすくする。
- 常時表示はタイトル、状態、担当者、主要操作などの最低限に絞り、画面説明や操作手順は「使い方」ポップアップで表示する。
- 新しい回覧の開始者は保存済みの氏名と「変更」だけを常時表示する。変更時だけ氏名・メール・部門検索を開き、選択した開始者を同じブラウザへ保持する。
- 案件一覧は案件名、案件ID、文書数、状態、現在担当または完了日時を一行で表示し、開始者や進捗バー等は詳細画面へ移す。
- 案件詳細の回付ルートでは結果と日時だけを一覧表示し、差し戻しも含めて詳細説明カードは常時表示しない。
- 案件詳細は「回付ルート・履歴」を最上部の主領域とし、独立した「現在の承認待ち」帯と対象文書カードは表示しない。現在の承認待ち者の行を水色で強調し、その行の中に対象文書と処理操作を表示する。
- 差し戻し時は既存ルートのハイライトを上へ戻さない。差し戻し直前までの処理結果を残し、「差し戻し後の回付ルート」を間に挟んで、戻り先から現在のルートを新しい連番で続ける。
- 同じ意味の項目を異なる表現で重複表示しない。
- 文字が小さすぎないよう視認性を優先する。
- 「案件情報」や「印影データは保存しません」のような説明専用カードは画面を煩雑にするため削除済み。再導入しない。
- 新しい回覧画面は、1「対象文書」→2「回付ルート」→「回覧を開始」の縦順。
- 案件詳細上部の「URLをコピー」「承認依頼メール」「回覧中」のボタン群は不要として削除済み。
- 装飾アイコンは使わない。氏名が表示されている箇所へ頭文字アイコンを重ねない。
- 案件詳細の指示、対象文書、処理ボタンは1つのまとまりとして表示する。
- 案件詳細の見出しに「対応が必要」の補助ラベルを表示しない。見出しと主操作だけで用件を伝える。
- 文書種別、処理種別、状態、回付順は、絵や記号ではなく短い文字ラベルと番号で表示する。
- 「捺印担当」「確認担当」の「担当」は不要。
- 閉じる、削除、並べ替え、メニューなどの操作は、記号だけにせず文字ボタンで示す。
- 濃い青は「承認待ち」「現在の承認待ち」「主操作」に限定し、重要度を色の強弱で判別できるようにする。

### 3.5 複数文書と捺印

- 1案件に複数文書を含められる。
- SharePoint文書は、利用者がSharePointでコピーしたリンクとファイル名を登録する。
- SharePoint内の自動一覧取得、Tenant ID、Client ID、`Sites.Selected`、管理者同意を使用しない。
- 案件画面の「文書を開く」は登録したSharePointリンクを別画面で開く。閲覧可否は会社側の既存アクセス権に任せる。
- リンク内の長いIDや記号を利用者が理解・入力する必要はなく、コピーしたリンクをそのまま貼り付ける。
- 各文書ごとに「捺印対象／確認のみ」を設定する。
- 案件詳細の対象文書一覧では、回付ルートと重複する「確認のみ」を表示しない。例外として注意が必要な「捺印対象」だけを表示する。
- 文書名の下に保存場所やフォルダ階層（例：`技術部 / 製品開発`）を表示しない。
- 例：PDFは捺印対象、Excelは確認のみ、を同一案件で扱える。
- 回付者にも「捺印必要／確認のみ」がある。
- 現在の回付者が捺印必要で、案件に捺印対象文書がある場合は「捺印と確認をお願いします」と表示する。
- 回付者の「捺印あり」は、案件内に捺印対象文書がある場合だけ有効とする。捺印対象文書が0件なら、既存データに捺印設定が残っていても「確認のみ」と表示する。
- PDF印影・署名画像をアプリ側へ保存しない。
- 利用者はAcrobat等で文書を開き、自分の署名・印影機能で捺印し、保存後にWebアプリへ戻る。
- 初期版では本当に捺印されたかを画像解析しない。

### 3.6 処理ボタンと履歴表現

- 捺印が必要な場合：`捺印・確認完了して回付`
- 確認のみの場合：`確認完了して回付`
- NG処理：`差し戻し`
- 処理済みメンバーの行には、結果（捺印・確認済み／確認済み）と処理日時を表示する。
- 「捺印済み・回付」「確認OK・回付」のような同義の履歴ラベルは重複するため削除済み。
- 差し戻しは回付ルート行へ結果と日時を表示する。戻り先・日時・任意コメントをまとめた赤い詳細カードは表示しない。
- 回付ルートと回付履歴は別々の大きな一覧にせず、「回付ルート・履歴」内で表現する。

### 3.7 差し戻し

- 戻り先は回覧開始者、または処理済みの前の承認者。
- 差し戻しコメントは任意。空欄でも差し戻せる。
- 空欄コメントは履歴に空の吹き出し等を表示しない。
- 差し戻し履歴にはNG実行者、戻り先、日時、その時点の状態を残す。
- コメントがあれば保存・表示する。

## 4. 全機能要件

### 4.1 基本回覧フロー

1. 対象ファイルを選択。
2. 回付ルートを設定。
3. 回覧開始。
4. 承認者へ順番に回付。
5. 承認者が文書を確認。
6. 必要な人は捺印。
7. OKなら次へ。
8. NGなら許可された任意の戻り先へ差し戻し。
9. 必要に応じて未処理の回付者を追加・削除・並べ替え。
10. 最後まで完了。

### 4.2 承認者マスタ

保持項目：`id`、氏名、メールアドレス、部門、Entra IDの`userId`、有効／無効、登録日時、更新日時。

管理操作：一覧、新規登録、編集、削除、無効化／有効化、氏名・メール・部門検索。

部門がMicrosoft Entra IDに存在しないユーザーも許容する。部門は現在任意入力に変更済み。

### 4.3 承認者オートコンプリート

- 1文字以上で候補表示。
- 氏名、メール、部門のどれでも検索。
- 300ms程度のdebounce。
- 候補には氏名、部門、メールをセット表示。
- 選択時は3項目をまとめて回付者として登録。
- 承認者マスタへの同一ユーザー重複登録は警告するが、回付ルートでは再確認のため同じ承認者を複数回追加できる。
- STEP 4ではローカル承認者マスタ候補とMicrosoft Graph社内名簿候補を統合。
- Graph候補は回付ルートへ直接追加でき、承認者マスタへ登録して再利用することもできる。

### 4.4 回付ルート

各メンバー：氏名、メール、部門、回付順、開始者区分、捺印必要／確認のみ、処理状態、処理日時。

操作：追加、同じ承認者の複数回追加、削除、ドラッグ＆ドロップ、上へ／下へ、捺印要否変更。

- 回覧開始者を回付ルートの先頭へ自動表示し、「開始者」ラベルを付ける。開始者は削除・並べ替え・捺印設定の対象外。
- 開始操作を開始者の処理結果として記録し、最初の承認者を現在担当にする。
- 開始者以外の行は、行全体のドラッグまたは「上へ／下へ」で並べ替えられる。

### 4.5 回付ルートテンプレート

操作：保存、読み込み、編集、上書き保存、名前を付けて保存、削除、複製。

`RouteTemplate`：`id`、`name`、`description`、`createdBy`、`createdAt`、`updatedAt`。

`RouteTemplateMember`：`routeTemplateId`、`approverId`、`sequence`、`requiresStamp`。

### 4.6 回覧開始時の案件データ

- 案件IDとランダムアクセスキー
- 任意の回覧名。未入力時は従来どおり文書名を表示名に使う
- ファイル保存先種別
- ファイルID、ファイル名、ファイルURL
- 複数文書一覧
- 回覧開始者、開始日時
- 開始者を先頭に含む回付ルートのスナップショット
- 案件状態
- 最初の承認者を現在担当に設定

### 4.7 案件一覧と詳細

元要件の分類：自分に回っている案件、自分が開始した案件、差し戻された案件、完了案件。

本人ログインを廃止したため、「自分」は認証ユーザーではなく案件URL運用上の表示・デモ選択として扱う。将来の分類方法は本人認証を再導入せずに再検討する。

詳細表示：ファイル名／複数文書、保存場所、回覧開始者、開始日時、現在の確認者、回覧状態、回付ルート、履歴、進捗率、文書を開く。

### 4.8 OK／NG／途中編集

- OK：現在メンバーを完了、次を現在担当へ。最後なら回覧完了。次の人へのメール下書きを作成。
- NG：開始者または処理済み前メンバーへ差し戻し。コメントは任意。差し戻し通知メール下書きを作成。
- 途中編集：未処理部分への追加、削除、並べ替え、捺印要否変更。処理済みは履歴保持。現在処理中の削除は警告。

### 4.9 回覧履歴

`CirculationHistory`想定項目：`id`、`caseId`、`actionUserId`、`actionUserName`、`action`、`comment`、`returnToUserId`、`previousState`、`newState`、`createdAt`。

回覧開始、確認完了、捺印・確認完了、差し戻し、ルート変更、完了等の重要操作を保存する。

## 5. ストレージ抽象化

回覧ロジックを保存先へ依存させない。

`StorageProvider`共通契約候補：`listFiles()`、`getFile()`、`openFile()`、`downloadFile()`、`updateFile()`、`getMetadata()`、`getWebUrl()`。

実装プロバイダー：`SharePointProvider`、`OneDriveProvider`、`SharedFolderProvider`。

SharePoint／OneDriveとSMB共有を無理に同じ内部実装にしない。外側の契約だけ揃える。

## 6. Microsoft 365・実ファイル方針

### SharePoint／OneDrive

- 管理者設定を不要にするため、Microsoft Graphによる自動一覧取得は採用しない。
- SharePointでコピーしたリンクを回覧へ登録し、元ファイルを別画面で直接開く。
- 閲覧可否は会社側で設定済みのSharePointアクセス権へ任せ、アプリから権限を追加・変更しない。
- Excel、Word、PowerPointはSharePoint／Office for the web、PDFはブラウザまたはAcrobat等で開く。
- OneDriveもSTEP 6で同じリンク登録方式へ統合する。

### 共有フォルダ

- 通常ブラウザからSMBへ直接アクセスしない。
- 会社PC上のバックエンドAPI、Windowsサービス、社内エージェント等を介する。
- SharePoint／OneDriveへ影響を与えず、`SharedFolderProvider`内部へ閉じ込める。

### 回覧データ保存

管理者設定を必要としない会社PCバックエンド保存方式をSTEP 8で再設計する。SharePoint Listsを前提に固定しない。

Dataverse、有料DB、有料SaaS、Premium Power Automateを勝手に前提にしない。Power Automateを使う場合も通知用途に限定し、回覧ロジック本体はWebアプリ側で管理する。

## 7. セキュリティ要件

- Microsoft 365パスワードを保存しない。
- クライアントシークレットをソースコードに書かない。
- ブラウザへ秘密情報やGraphアクセストークンを渡さない。
- `.env`を利用し、Gitへコミットしない。
- `.env.example`だけをコミットする。
- 証明書、秘密鍵、`.pem`、`.pfx`、`.key`をコミットしない。
- 印影・署名画像をサーバーへ保存しない。
- Graph権限はSTEPごとに最小限。
- STEP 4のGraph社内名簿検索は任意機能。管理者へ依頼しない運用では設定せず、ローカル承認者マスタを使う。
- SharePoint文書は`https`かつ`sharepoint.com`のリンクだけを登録し、アプリからアクセス権を変更しない。
- 本運用はクライアントシークレットより証明書認証を優先。
- 本人認証なしのため、社内名簿APIや案件URLを公開インターネットへ無防備に公開しない。社内LAN・VPN・ファイアウォール等で範囲を限定する。

## 8. 段階開発ルール

- 最初から全機能を一括実装しない。
- STEPを飛ばさない。
- Microsoft 365接続前にUIモックを完成させる。
- 各STEP完了時に「実装した内容」「動作確認方法」「現時点の制約」「次のSTEPで行う内容」を報告し、ユーザーが確認できる状態で区切る。
- 同一STEP内の単純なコード修正ごとに確認を求める必要はない。
- 不明点を勝手に本番仕様として固定しない。
- 有料サービスを勝手に採用しない。
- 過度な独自フレームワークを作らない。

## 9. STEP一覧と現在地

- [x] STEP 0：設計
- [x] STEP 1：完全ローカルUIモック
- [x] STEP 2：Git管理用ファイル整備・GitHub Pages公開
- [x] STEP 3：利用者ログイン不要の案件URL方式へ変更
- [x] STEP 4：Microsoft Graphユーザー検索（任意機能、通常運用はローカル承認者マスタ）
- [x] STEP 5：SharePointリンク登録と実ファイル起動
- [ ] STEP 6：OneDriveリンク登録
- [ ] STEP 7：OneDrive／共有フォルダの実ファイル起動
- [ ] STEP 8：会社PCバックエンドへ回覧情報保存
- [ ] STEP 9：実ユーザーで実際の回覧
- [ ] STEP 10：実データでNG・差し戻し
- [ ] STEP 11：実データで途中ルート編集
- [ ] STEP 12：共有フォルダ対応
- [ ] STEP 13：実メール通知（管理者設定不要の方式を再検討）

STEP 1のUIモックにはSTEP 9～11相当の操作感もローカルデータで先行実装されている。後続STEPは、実データ・実ユーザー・永続化へ置き換える段階を意味する。

## 10. 実装済みの主な画面・操作

- 案件一覧を内包したホーム、別ウィンドウで開く承認者マスタと新しい回覧、案件詳細。
- 承認者の追加、編集、削除、無効化、検索。
- 300ms debounceの承認者オートコンプリート。
- 複数ダミー文書選択と文書ごとの捺印要否。
- 回付ルート作成、追加、削除、ドラッグ、上下移動、捺印要否変更。
- ルートテンプレート保存、読込、上書き、複製、削除。
- 案件開始とランダム案件URL発行。
- 現在の確認者、進捗、回付ルート内履歴。
- 確認完了／捺印・確認完了。
- NG差し戻し、戻り先選択、任意コメント。
- 未処理ルートの途中編集。
- Webアプリ内メール作成・編集・送信確認デモ。
- LocalStorage保存とデモデータ初期化。
- サイドバーを使わず、PC・スマホ共通のホーム上部ボタンから各管理画面を別ウィンドウで開く導線。

## 11. リポジトリと公開先

- リポジトリ：`https://github.com/waragamef-png/approval-circulation-app.git`
- ブランチ：`main`
- GitHub Pages：`https://waragamef-png.github.io/approval-circulation-app/`
- STEP 4完成コミット：`9b9bb80 STEP 4の社内名簿検索と回覧UIを完成`
- 公開確認後の修正：`12956a7 初期案件にも回覧開始者を補完`

直近コミット：

```text
12956a7 初期案件にも回覧開始者を補完
9b9bb80 STEP 4の社内名簿検索と回覧UIを完成
5acc03f 回付履歴と差し戻し操作を整理
5151b60 案件詳細の表示を4点整理
9b6f0c9 回付ルートに履歴と処理アイコンを統合
35ba6f6 スマホメニューと回覧開始画面を整理
cf89e9e 複数文書対応と画面表示を整理
b33d91c Docs公開デモの注意事項を追加
6083bb0 DeployデモをGitHub Pagesで公開
5eb5a3c Initial社内承認回覧UIモック
```

公開版はダミーデータだけを使う画面確認用。個人情報はダミー。会社本番運用にはまだ使わない。

## 12. 現在のGit状態（非常に重要）

2026-08-30にSTEP 5を管理者設定不要のSharePointリンク方式へ変更し、機能コミット`e444f70`を`main`へpush済み。GitHub Actions `33318214052`のPagesビルド・配信成功と公開版の表示を確認した。この文書の完了状態更新を含め、次回再開時の`git status --short`は空であることが正常。

今後も未確認の変更がある場合は、リセット、checkout、削除で破棄しない。

## 13. STEP 4で現在実装されている内容

### バックエンド

追加済み：`server/index.mjs`、`server/graphAuth.mjs`、`server/graphDirectory.mjs`、`server/graphDirectory.test.mjs`。

- Express API。
- `GET /api/health`。
- `GET /api/directory/users?q=<検索語>&limit=<件数>`。
- MSAL NodeのConfidential Clientでアプリ認証。
- 証明書認証を優先し、クライアントシークレットは一時的な代替として対応。
- Graph `/users`から必要最小限の項目だけ取得。
- 無効ユーザーを除外。
- `mail`がない場合は`userPrincipalName`を使用。
- 部門なしを許容。
- 氏名・メール・部門を途中一致で検索。
- 既定最大5,000名、既定5分キャッシュ。
- 設定なしの場合は503と日本語メッセージ。
- ビルド済み`dist`があれば会社PC用Web画面も3000番から配信。

### フロントエンド

追加済み：`src/services/directory.ts`。

- 回付ルートの候補でローカルマスタとGraph社内名簿を統合。
- Graph候補には「社内名簿」表示。
- Graph候補を回付ルートへ直接追加可能。
- 承認者マスタに「社内名簿から追加」ボタンと検索モーダル。
- Graph利用者を承認者マスタへ登録。
- `entraUserId`またはメールで重複防止。
- Graph失敗時もローカル候補は継続利用。
- Entra IDに部門がないユーザーに対応するため、承認者フォームの部門を任意に変更。

### 起動構成

- `pnpm dev`：APIとViteを同時起動。
- `pnpm dev:web`、`pnpm dev:api`。
- `pnpm start`：会社PC配信用Express。
- `pnpm test`：Node標準テスト。
- 開発時の`/api`を`http://127.0.0.1:3000`へproxy。
- 会社PC配信時は`VITE_PUBLIC_BASE_PATH=/`。
- GitHub Actionsでは`/approval-circulation-app/`をbaseとして維持。
- `VITE_API_BASE_URL=same-origin`なら、別PCからアクセスしても閲覧PC自身の`localhost`へ誤接続しない。

## 14. STEP 4で確認済み／未確認の内容

2026-08-16に新PCの`C:\Users\win1\Documents\Codex\app\approval-circulation-app`で再検証済み：

- `.git`とリポジトリ直下を確認し、親の`app`リポジトリと混同していない。
- コピーされた旧`node_modules`のリンク不整合を確認し、旧内容を一時フォルダへ退避してからロックファイルどおりに再生成した。ソース差分は破棄していない。
- 新PCのシステムNode.jsは`v20.11.1`だったため、検証にはCodex同梱のNode.js `v24.19.0`を使用した。通常開発用にはNode.js 22を推奨する。
- `pnpm install --frozen-lockfile`成功。
- `pnpm typecheck`成功。
- `pnpm test`成功。
- テスト内容：氏名・メール・部門の途中一致、ページング、無効ユーザー除外、UPNフォールバック、部門なし、キャッシュ。
- 会社PC向け`pnpm build`成功。
- GitHub Actions相当のPages向けbase pathビルド成功。
- `git diff --check`成功。
- `pnpm dev`でAPI（3000番）とVite（5173番）の同時起動成功。
- 未設定時の`GET /api/health`：HTTP 200、`{"status":"ok","graphConfigured":false}`。
- 未設定時の社内名簿API：HTTP 503、`Microsoft 365連携が未設定です。`。
- `pnpm start`で会社PC配信用`http://127.0.0.1:3000/`と案件URL形式`/c/<accessKey>`がHTTP 200。
- ブラウザで「社内名簿から追加」、未設定表示、Graphエラー時のローカル候補継続、ローカル候補追加、部門任意登録を確認。
- 検索文字を消したときの検索状態解除と、Entra ID・メールによる名簿候補重複除外を追加確認・修正した。
- ブラウザの警告・エラーログなし。水色基調のPC表示に横方向のはみ出しなし。
- ソースへ秘密情報、秘密鍵、証明書が追加されていないことを確認。

2026-08-29から30日に同じ新PCでUI簡素化を実施・確認済み：

- ページ見出し下の説明文を常時表示せず、「使い方」ポップアップへ移した。
- サイドバーを撤去し、ホームへ案件一覧を統合した。ホーム上部の「新しい回覧」「承認者マスタ」だけを別ウィンドウで開く構成にした。
- ホーム、承認者一覧、新しい回覧、案件詳細の余白とカード高を縮めた。
- 案件一覧から開始者、保存場所、進捗バーを外し、現在担当を含む一行表示へ変更した。
- 案件詳細の通常システム履歴と差し戻し詳細カードを非表示にし、処理結果と日時だけを表示した。
- 装飾アイコン、氏名横の頭文字アイコン、チェック・検索・矢印・×のみの操作を削除し、短い日本語ラベルへ統一した。
- 承認待ちタブ、現在の承認待ち、主操作のコントラストを強め、重要箇所を判別しやすくした。
- 文書の保存場所表示を削除し、捺印対象文書がない案件では全回付者を「確認のみ」と表示するよう整合させた。
- 案件詳細の「対応が必要」と、対象文書行の冗長な「確認のみ」を削除した。対象文書行では「捺印対象」の場合だけ表示する。
- 案件一覧の状態タブは、クリック後にブラウザ既定の黒枠を残さず、選択中を青い下線だけで示す。件数は各タブ内の文字バッジとして表示する。
- 新しい回覧に任意の回覧名を追加し、未入力時は文書名へフォールバックする。開始者は回付ルート先頭の固定行として表示・記録し、開始者以外は行ドラッグと「上へ／下へ」の両方で並べ替えられる。
- 開始者の常時プルダウンを廃止し、保存済み氏名と「変更」ボタンへ変更した。「変更」で氏名・メール・部門検索を開き、社内名簿から選んだ開始者も承認者マスタへ追加してLocalStorageへ保持する。
- 回覧開始直後にも、最初の回付者を宛先とする承認依頼メールの下書きを自動表示する。案件名、文書数、案件ID、対応内容、案件専用URLを自動入力し、送信前に編集できる。
- 案件詳細の青い「現在の承認待ち」帯と独立した対象文書欄を削除し、「回付ルート・履歴」を案件名直下へ移した。現在担当行を水色で強調し、対象文書・文書を開く・完了・差し戻し操作をその行へまとめた。差し戻し中は開始者行、完了後は最後の処理者行に文書を表示する。
- 差し戻し直前の回付ルートを履歴へスナップショット保存するようにした。表示では差し戻した担当者までの結果を残し、その下へ「差し戻し後の回付ルート」を挿入して、戻り先以降を新しい連番で再表示する。複数回の差し戻しも同じ区切りで時系列表示する。
- 回覧開始、通常回付、完了、差し戻し、再回覧は、メールの「送信を確認」を押すまで案件へ反映しないようにした。メール画面をキャンセルした場合は処理結果、履歴、現在の承認待ちを変更しない。
- 再確認が必要な承認者を同じ回付ルートへ複数回追加できるようにした。各回付行を個別IDで処理し、同じ人が複数いる場合も現在担当、処理結果、差し戻し先を回付順ごとに区別する。
- 実ブラウザで同じ承認者を2回追加して回覧を開始し、1回目の完了後に同じ人の2回目が承認待ちになること、差し戻し先で同じ人の回付順を区別できること、未処理ルート編集でも再追加できることを確認した。
- 回付ルート行の下に重複表示していた赤い差し戻し詳細カードを削除し、差し戻し結果と日時は行右側だけに表示するよう整理した。差し戻しデータ自体は履歴へ保持する。
- メール作成画面にCC欄を追加し、過去の承認者を自動入力するようにした。同一人物がルートへ複数回登場する場合はメールアドレスで重複を除き、宛先と同じアドレスもCCへ入れない。
- 実ブラウザで同じ承認者が複数回登場する案件を回付し、次の宛先と同じメールアドレスはCCが空になること、完了通知では同じ承認者のアドレスがCCへ1件だけ入ることを確認した。
- `pnpm typecheck`、`pnpm test`、会社PC向け`pnpm build`、`git diff --check`成功。
- 実ブラウザで主要4画面と説明ポップアップを確認し、横方向のはみ出しとコンソールエラーがないことを確認した。
- `9b9bb80`を`main`へpushし、GitHub Actions `33315201584`のPagesビルド・配信成功を確認した。
- 公開版の初回表示で旧デモ案件だけ開始者行が補完されない問題を発見し、初期データ、保存済みデータ、初期化後データへ同じ補完処理を適用するよう`12956a7`で修正した。
- `12956a7`のGitHub Actions `33315450320`のPagesビルド・配信成功を確認した。公開版で開始者行、現在の承認待ち、メールCC欄、警告・エラーログなしを確認した。

任意機能として保留：

- Graph社内名簿の実テナント疎通。管理者へ依頼しないという最新判断により、通常運用ではローカル承認者マスタを使う。ユーザーが将来明示的に希望しない限り、管理者情報の準備を求めない。

## 15. STEP 5で実装・確認した内容

`19a807d`で実装したGraph自動一覧取得方式は、その後のユーザー判断により不採用。管理者問い合わせを必要としないリンク方式へ置き換えた。

機能コミット：`e444f70 SharePoint参照をリンク方式へ変更`

### 最新の実装

- 「新しい回覧」に「SharePointリンクを追加」を表示。
- SharePointでコピーした`https://*.sharepoint.com/`リンクを貼り付ける。
- URL内にファイル名が含まれる場合は自動入力し、共有用の不透明なリンクでは利用者がファイル名だけ入力する。
- 同じSharePointリンクの重複登録を防止。
- 1案件へ複数リンクを追加し、文書ごとの「捺印対象／確認のみ」を維持。
- 案件画面の「文書を開く」は登録したSharePointリンクを別画面で開く。
- 閲覧可否はSharePoint側の既存アクセス権で判断し、アプリから権限を追加・変更しない。
- Graph SharePoint API、`Sites.Selected`、SharePoint用Tenant ID・Client ID・証明書、対象サイト権限を不要にした。
- `server/graphSharePoint.mjs`、`server/graphSharePoint.test.mjs`、`src/services/sharepoint.ts`、`GET /api/sharepoint/files`、SharePoint専用環境変数を削除。
- STEP 4のGraph社内名簿検索は任意機能として残す。管理者へ依頼しない運用では、ローカル承認者マスタだけを使用する。

### 確認内容

- SharePointリンク貼り付け時の日本語ファイル名自動入力。
- ファイル名を含まない共有リンクへの手入力。
- PDFとExcelの複数文書追加。
- 同一リンクの重複防止。
- 回覧開始、メール確認、案件保存後もリンクを保持。
- 案件画面のリンク先が登録したSharePoint URLと一致し、別画面指定になっていること。
- 既存ダミー案件は実リンクへ移動せず、確認用メッセージを表示すること。
- `e444f70`のGitHub Actions `33318214052`でPagesビルド・配信成功。
- 公開版で「SharePointリンクを追加」、入力項目がリンクとファイル名だけであること、確認用SharePoint URLから日本語ファイル名が自動入力されることを確認。実在する社内リンクや情報は使用していない。

## 16. 次に再開時に行うこと

1. `git status --short`が空で、`git log --oneline -5`にSTEP 5のSharePointリンク方式完成コミットがあることを確認する。
2. STEP 5は管理者設定不要のリンク方式として、実装・検証・コミット・push・GitHub Pages反映まで完了済みとして扱う。
3. ユーザー確認後、別の区切りでSTEP 6「OneDriveリンク登録」へ進む。SharePointと同じリンク入力画面へ統合する。
4. Microsoft 365管理者への問い合わせを前提にしない。Graph社内名簿検索は任意のまま保留し、通常運用はローカル承認者マスタで進める。

```powershell
cd <新PCのコピー先>\app\approval-circulation-app
git status --short
git diff --check
pnpm install
pnpm typecheck
pnpm test
pnpm build
pnpm dev
```

## 17. 任意のGraph社内名簿検索を使う場合だけ必要なもの

- Directory（Tenant）ID
- Application（Client）ID
- Application `User.Read.All`
- テナント管理者の同意
- 証明書の公開鍵をアプリ登録へアップロード
- 会社PC側の秘密鍵ファイルのパス
- 証明書のSHA-256拇印

これらは任意のGraph社内名簿検索を有効にする場合だけ必要。管理者へ依頼しない通常運用では設定しない。SharePoint文書リンクの登録・閲覧には不要。

秘密鍵ファイルやクライアントシークレットをチャットへ貼らせない。`.env`もGitへコミットしない。

主な環境変数：

```dotenv
VITE_API_BASE_URL=same-origin
VITE_PUBLIC_BASE_PATH=/
APP_HOST=127.0.0.1
APP_PORT=3000
APP_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
GRAPH_TENANT_ID=
GRAPH_CLIENT_ID=
GRAPH_CLIENT_CERTIFICATE_PATH=
GRAPH_CLIENT_CERTIFICATE_THUMBPRINT=
GRAPH_CLIENT_SECRET=
GRAPH_DIRECTORY_CACHE_TTL_MS=300000
GRAPH_DIRECTORY_MAX_USERS=5000
```

会社LANの別PCから使う段階では、`APP_HOST=0.0.0.0`、Windowsファイアウォール、固定IPまたは社内DNS、許可接続元を社内管理者と設定する。

## 18. データモデル上の重要事項

- 主要型は`src/types.ts`。
- `Approver`、`RouteMember`、`RouteTemplate`、`HistoryEntry`、`CaseDocument`、`CirculationCase`、`AppData`、`MockFile`。
- `CirculationCase.title`は任意の回覧名。空の場合は文書名へフォールバックする。
- `RouteMember.isInitiator`で回付ルート内の開始者を区別する。テンプレート保存時は開始者を除外し、利用時の開始者を自動挿入する。
- `HistoryEntry.routeSnapshot`は差し戻し直前の回付ルートを保持し、差し戻し前後のルートを時系列で分けて表示するために使用する。
- 案件開始時に氏名、メール、部門、捺印要否を案件メンバーへスナップショット保存する。
- 承認者マスタが変更されても過去案件の履歴表示を変えない。
- 1案件に`documents: CaseDocument[]`を持つ。
- SharePointリンク方式では`CaseDocument.fileId`へアプリ内のランダムID、`fileUrl`へコピーしたSharePointリンクを保存する。
- 案件に`accessKey`を持つ。
- STEP 1のLocalStorageキーは`circlia-step1-v1`。内部旧名だが、データ移行なしで不用意に変更しない。

## 19. 現時点の制約

- 案件、テンプレート、履歴はまだブラウザのLocalStorage。
- 選択した開始者も現在のブラウザだけに保持される。別PC、別ブラウザ、プライベートブラウズ、ブラウザデータ消去後は再選択が必要。
- 複数PC間で案件データを共有できない。
- GitHub Pages版はバックエンドを持たず、Graph検索や実メール送信をしない。
- SharePoint文書は自動一覧取得せず、利用者がコピーしたリンクを登録する。
- リンクを開いた後の閲覧可否はSharePoint側の既存アクセス権に従う。
- SharePointへ未ログインの場合は、SharePoint側で会社アカウントのログインを求められる場合がある。
- OneDrive、SharePoint Lists未接続。
- メール送信はUIデモ。
- PDF捺印有無の自動判定なし。
- 同時更新、排他制御、ネットワーク障害対策、監査ログ本番化は未実装。
- 本人認証なしのため、案件URLを持つ人が操作できる。
- Graphキャッシュは既定5分、上限5,000名。大規模テナントでは再設計が必要。

## 20. やってはいけないこと

- いきなり全STEPを実装しない。
- STEP 5未完了のままSTEP 6へ進まない。
- 本人認証やMicrosoft 365利用者ログインを勝手に復活させない。
- URL方式を個人アカウント方式へ変更しない。
- `mailto:`方式へ戻さない。
- 印影画像をアプリへ保存しない。
- 文書ごとの捺印要否を失わせない。
- 重複ラベル、不要カード、中途半端な英語を増やさない。
- 処理済み行へ「確認OK・回付」等を再表示しない。
- 差し戻しコメントを必須へ戻さない。
- Dataverse、有料DB、有料SaaS、Premium Power Automateを勝手に採用しない。
- シークレット、秘密鍵、Microsoft 365パスワードをソースへ書かない。
- 未コミット変更を破棄しない。
- ユーザーの明示なしに別GitHubリポジトリへpushしない。

## 21. STEP完了報告の形式

各STEP完了時は次を報告する。

### 実装した内容

- 何が操作できるようになったか。

### 動作確認方法

- 起動コマンド、ブラウザURL、具体的な操作。

### 現時点の制約

- ダミー／実接続の区別、必要な社内設定、未検証事項。

### 次のSTEPで行う内容

- 次の1STEPだけを説明し、ユーザーが確認できる状態で一度停止。

## 22. 参照すべきファイル

- `README.md`：一般向け説明と起動手順
- `HANDOFF_PROMPT.md`：新しいスレッドへ貼る開始プロンプト
- `PROJECT_HANDOFF.md`：本引継ぎ文書
- `src/App.tsx`：画面・回覧操作
- `src/types.ts`：データモデル
- `src/mockData.ts`：ダミーデータ
- `src/storage.ts`：LocalStorage
- `src/styles.css`：UI
- `src/providers/StorageProvider.ts`：ストレージ抽象化
- `src/services/directory.ts`：Graph社内名簿APIクライアント
- `server/index.mjs`：会社PC用API／Web配信
- `server/graphAuth.mjs`：MSALアプリ認証
- `server/graphDirectory.mjs`：Graphユーザー取得・検索
- `.env.example`：秘密情報を含まない設定例
- `package.json`：起動・検証コマンド

## 23. 代表的な回帰確認操作

1. 承認者を氏名・メール・部門で検索し追加。
2. SharePointリンクを複数追加し、文書ごとの捺印要否を設定。
3. 回付ルートを作成、並べ替え、捺印要否変更。
4. ルート保存、読み込み、上書き、複製。
5. 回覧開始し、案件URLと現在確認者を確認。
6. 確認完了または捺印・確認完了。
7. 次の人へのメール作成画面を編集。
8. NG差し戻し。コメント空欄でも実行。
9. メールに「コメント：なし」が入ることを確認。
10. 未処理ルートへ途中追加・削除・並べ替え。
11. 最後まで回覧完了。
12. スマホ幅でもホーム上部の2ボタン、案件一覧、回付ルートの番号・処理ラベル・文字ボタンの配置を確認。

この一連とローカル承認者マスタを維持した状態で、STEP 5のSharePointリンク登録・実ファイル起動が使えるのが正しい完成形です。
