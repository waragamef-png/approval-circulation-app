# 新しいPC・新しいスレッド用の開始プロンプト

以下の「コピーして貼り付ける範囲」を、新しいPCで開いたCodexの新しいスレッドへそのまま貼り付けてください。

## コピーして貼り付ける範囲

```text
社内承認回覧Webアプリの開発を、以前のPC・スレッドから引き継いでください。

プロジェクトフォルダは、今回コピーした `app` フォルダ内の `approval-circulation-app` です。新しいPC上の実際の絶対パスを確認し、そこを作業ディレクトリにしてください。

最初に必ず、プロジェクト直下の `PROJECT_HANDOFF.md` を省略せず最後まで読んでください。次に `README.md`、`package.json`、`.env.example`、`git status --short`、`git diff --check`、`git diff --stat`、`git log --oneline -10` を確認してください。

重要事項：

- STEP 6完了時点では作業ツリーが空であることが正常です。もし未コミット変更があれば、内容を確認せず破棄しないでください。
- `git reset --hard`、`git checkout --`、未確認のファイル削除は行わないでください。
- 最新のユーザー判断は、最初の原案より優先されます。
- 本人確認・利用者ログインは不要です。Microsoft 365ログインを再導入しないでください。
- Microsoft 365管理者への問い合わせを前提にしないでください。SharePoint／OneDrive文書は自動一覧取得せず、各サービスでコピーしたリンクとファイル名を登録します。
- 文書参照用のTenant ID、Client ID、`Sites.Selected`、Drive ID、対象サイト権限は使用しません。案件画面でリンクを開き、閲覧可否は会社側の既存アクセス権へ任せます。
- 1案件ごとに推測されにくい専用URLを割り当て、URLをメールで共有する方式です。
- 案件画面の先頭には、現在誰の承認待ちかを表示します。
- メーラー起動方式ではなく、Webアプリ内でメール内容を確認・編集し、送信ボタンで送る方式です。
- 現在のGitHub Pages版は画面確認用で、ダミーデータのみです。実際の会社情報やSharePoint／OneDriveリンクを登録しないでください。
- 実運用は会社PCを社内Webサーバーとして使う方向です。
- UIは水色基調、簡潔な日本語、PC優先です。中途半端な英語や重複表現を増やさないでください。
- GUIは最低限の情報で一覧できる構成を優先し、説明や操作手順は「使い方」ポップアップへ分離してください。
- サイドバーは使わず、ホームに案件一覧をそのまま表示してください。ホーム上部には「新しい回覧」「承認者マスタ」の2ボタンを置き、どちらも別ウィンドウで開いてください。
- 装飾アイコンや氏名横の頭文字アイコンは使わず、文書種別・状態・操作は短い日本語ラベルで示してください。記号だけの操作ボタンも増やさないでください。
- 文書名の下に保存場所やフォルダ階層を表示しないでください。捺印対象文書がない案件では、回付者の処理種別も「確認のみ」と表示してください。
- 案件詳細の見出しに「対応が必要」を表示しないでください。対象文書一覧では「確認のみ」を表示せず、「捺印対象」の場合だけラベルを表示してください。回付ルート側の処理種別表示は残してください。
- 複数文書を1案件に含められ、文書ごとに「捺印対象／確認のみ」を設定します。
- 新しい回覧では回覧名を任意入力でき、未入力なら文書名を表示名に使ってください。回覧開始者は回付ルートの先頭へ固定表示し、開始者以外の行はドラッグまたは「上へ／下へ」で並べ替えられる状態を維持してください。
- 再確認が必要な人を再度回付できるよう、同じ承認者を1つの回付ルートへ複数回追加できる状態を維持してください。現在担当、履歴、差し戻し先は回付行ごとに区別してください。
- 回覧開始者は保存済みの氏名と「変更」だけを常時表示し、常時プルダウンへ戻さないでください。「変更」を押したときだけ氏名・メール・部門検索を開き、同じブラウザでは選択を保持してください。
- 回覧開始直後は、最初の回付者を宛先とする承認依頼メールの下書きをアプリ内で自動表示し、送信前に宛先・件名・本文を編集できる状態を維持してください。
- 回覧開始、通常回付、完了、差し戻し、再回覧では、メールの「送信を確認」を押したときだけ案件・履歴・現在の承認待ちを変更してください。メール画面をキャンセルした場合は何も変更しないでください。
- メール作成時は過去の承認者をCCへ自動入力してください。同じ人がルートへ複数回登場してもメールアドレスは1件にまとめ、宛先と同じアドレスをCCへ重複させないでください。
- 案件詳細では独立した「現在の承認待ち」帯と対象文書カードを再導入しないでください。「回付ルート・履歴」を案件名直下へ置き、現在の承認待ち者の行を水色で強調して、その行の中へ対象文書と処理操作をまとめてください。
- 差し戻し時に既存ルートのハイライトを上へ戻さないでください。差し戻し直前の結果を残し、その下に「差し戻し後の回付ルート」を挟んで、戻り先から現在のルートを新しい連番で続けてください。複数回の差し戻しも同様に時系列表示してください。
- PDF印影データはアプリに保存しません。
- 差し戻しコメントは任意です。
- 処理済みメンバーには結果と日時だけを表示し、「確認OK・回付」「捺印済み・回付」のような重複履歴ラベルや、差し戻しの赤い詳細カードは表示しないでください。差し戻しデータ自体は履歴へ保持します。

現在の再開位置はSTEP 6「OneDriveリンク登録」の完了後です。SharePoint／OneDriveとも管理者設定を必要としないリンク方式を採用し、ローカル検証、コミット、push、GitHub Pages反映まで完了しています。ユーザー確認なしにSTEP 7へ進まず、まずPROJECT_HANDOFF.mdの「次に再開時に行うこと」に従って状態を確認してください。

再開時は、まず次を報告してください。

1. 読み取った現在のGit状態
2. 保持すべき未コミット変更
3. STEP 6のSharePoint／OneDriveリンク方式と確認済み内容
4. これから行う検証
5. 管理者情報が不要な範囲と、ユーザーが確認する操作

秘密情報をソースコードへ書かず、秘密鍵やクライアントシークレットをチャットへ貼るよう依頼しないでください。必要になった値だけ、用途を平易な日本語で説明して確認してください。

段階開発ルールを厳守してください。各STEP完了時に「実装した内容」「動作確認方法」「現時点の制約」「次のSTEPで行う内容」を報告し、ユーザーが確認できる状態で一度区切ってください。同一STEP内の単純修正ごとに確認を求める必要はありません。
```

## 新しいPCで一緒に渡すもの

- `C:\Users\win1\Documents\Codex\app` のコピー
- 特に `approval-circulation-app\.git` を含む隠しファイル一式
- この `HANDOFF_PROMPT.md`
- `PROJECT_HANDOFF.md`

`node_modules` はコピーされていても再生成可能です。新しいPCではNode.jsとpnpmを準備し、プロジェクト直下で `pnpm install` を実行してください。
